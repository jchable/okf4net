---
type: C# Member
title: Boxed.Render
description: Renders the box. The doc comment carries Corner and markdown-significant characters -- [brackets], a `backtick` and a <tag> -- so the neutralisation layer is exercised by the golden rather than only by unit tests.
resource: https://example.com/acme/fixture/blob/main/src/Shapes.cs#L61-L66
tags:
  - csharp
  - member
  - public
description_source: doc-comment
generated:
  by: okfgen/0.1.0
---

# Boxed.Render

Renders the box. The doc comment carries Corner and markdown-significant characters -- [brackets\], a `backtick` and a <tag> -- so the neutralisation layer is exercised by the golden rather than only by unit tests.

## Signatures

- `public string Render()` — `src/Shapes.cs#L61-L66`

## Calls (unresolved)

- `Compose`
