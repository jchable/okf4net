# OKF spec gap report — 2026-07-31

Generated by `/spec-gap-report`. Compares the upstream **OKF v0.2** spec
(`GoogleCloudPlatform/knowledge-catalog`, `okf/SPEC.md`, fetched live from
`main` on 2026-07-31) against **OKF4net** at commit `e5d8e4c` (2026-07-31,
post-0.4.0 `dev`), which targets OKF spec **v0.2** (`OkfSpec.Version =
"0.2"`, `src/OKF4net/OkfSpec.cs:13`).

This is a report for a human to act on — not a CI check, not an executable
test suite. It never suggests touching `tests/fixtures/` (byte-exact golden
captures) to resolve any finding found here.

## Summary

- **Upstream spec version:** v0.2 (declared: `specifies OKF version **0.2**`).
- **OKF4net's latest tracked spec version** (README's version↔spec table,
  latest row `0.4.0`): v0.2.
- **Version drift: none.** Upstream and OKF4net's latest tracked row agree
  on v0.2 — see §1 below.
- **43 atomic statements** extracted across 13 top-level spec sections
  (§1–§13; §1 Motivation and §2 Terminology yielded none — pure
  goals/definitions prose, no modal-keyword or declarative-requirement
  markers).

| Status ↓ / Severity → | Critical | Major | Minor | (N/A, no severity) | Total |
|---|---|---|---|---|---|
| Implemented | 4 | 14 | 8 | — | 26 |
| Partial | 0 | 6 | 0 | — | 6 |
| Missing | 0 | 0 | 3 | — | 3 |
| Diverges / Undocumented | 1 | 0 | 0 | — | 1 |
| Diverges / Intentional | 0 | 2 | 0 | — | 2 |
| N/A | — | — | — | 5 | 5 |
| **Total** | **5** | **22** | **11** | **5** | **43** |

Note on the `Partial` vs. `Diverges`/Undocumented line: three statements
(`sources[].resource` REQUIRED, `generated.by` REQUIRED, ISO-8601 `log.md`
date headings MUST) are detected-but-only-`Warning` in the same way §11-3
below is — but per this skill's own tie-breaker ("when in doubt, default
to `Partial`: `Diverges` is the stronger claim and needs unambiguous
evidence of *conflicting*, not just incomplete, behavior"), these three
are classified `Partial`: nothing in the spec says a REQUIRED sub-field's
absence must hard-fail validation of the whole document, so downgrading
to `Warning` is incomplete enforcement, not an active conflict. §11-3 is
different and stays `Diverges`/Undocumented: it contradicts an explicit
declarative §11 conformance condition *and* the project's own planning
doc's stated intent for that exact behavior — unambiguous conflict, not
just incompleteness.

**Headline findings for a human to act on:**

1. **§11-3 (Critical, Diverges/Undocumented):** malformed reserved files
   (`index.md`/`log.md` that violate §8/§9 structure, or that fail to parse
   at all) never fail conformance in `BundleValidator` — they're `Warning`
   or silently skipped, even though this is one of §11's three declarative
   conformance conditions and the project's own `docs/superpowers/plans/
   2026-07-27-okf-v0.2-core-and-cli.md:18` planning doc says malformed
   reserved files were meant to be `Error`-level. This is the report's one
   undocumented, Critical-severity gap. See §11 detail below.
2. **§5.1-1 / §5.2-1 (Major, Partial):** `sources[].resource` and
   `generated.by` are both spec-`REQUIRED` sub-fields, but OKF4net only
   ever flags their absence as `Warning`, never blocks — detected and
   named, just not enforced yet. Unlike the §13.1 legacy-field renames
   (which have a dedicated, revision-tracked design citation — see below),
   there's no field-specific documented rationale for the current
   Warning-only weighting, so a future PR that decides to tighten this
   would be closing a real (if lower-severity) gap, not reversing a
   documented decision.
3. **§9-2 (Major, Partial):** `log.md` date headings that
   aren't ISO-8601 are detected and named but only ever `Warning`, with no
   documented rationale either.
4. Everything else in the report is either solidly `Implemented`, a
   reasonable `Partial` (e.g. §10's Attested Computation checks are
   detected but kept outside the §11 conformance floor — that trade-off
   *is* documented in a section-specific code comment), or one of the two
   well-known, well-documented §13.1 intentional divergences.

---

## Version-drift finding (§1)

```sh
$ grep -m1 -oE 'specifies OKF version \*\*[0-9]+\.[0-9]+\*\*' SPEC.md
specifies OKF version **0.2**
```

README.md's `### OKF4net version ↔ OKF spec version` table's latest row:

```
| [0.4.0](CHANGELOG.md#040---2026-07-30) | v0.2 | ... |
```

**No drift.** Upstream v0.2 == OKF4net's latest tracked v0.2. No finding.

---

## Per-section detail

Each row: **statement** → **status** (severity) → citation with quoted
snippet → reasoning. `Diverges` rows are sub-classified **Intentional**
(documented choice) or **Undocumented** (real, unaddressed gap).

### §3 Bundle structure

README mapping: `OKF4net.Bundle`, `Bundle.ReservedFilenames`

- **S3-1** (MAY, distribution mechanism — git repo / tarball / subdirectory) —
  **N/A**. `src/OKF4net/Bundle.cs:106`: `public static Bundle Load(string
  root)` — takes an arbitrary directory path with no notion of how it got
  there; a repo-wide grep for `tarball`/`archive`/`git repository` in
  `src/OKF4net` returns no hits. Distribution mechanism is a
  deployment-time fact, not something a C# library implements.
- **S3-2** (§3.1, MUST NOT, reserved filenames used as concepts) —
  **Implemented** (Major). `src/OKF4net/Bundle.cs:137-145`:
  ```csharp
  var filename = System.IO.Path.GetFileName(path);
  switch (filename) { case IndexFilename: indexFiles.Add(path); break;
  case LogFilename: logFiles.Add(path); break; default: /* parsed into Concept */ }
  ```
  `CollectMarkdown` recurses into every subdirectory and the switch keys
  purely on filename (not full path), so `index.md`/`log.md` are diverted
  regardless of nesting depth. `tests/OKF4net.Tests/BundleTests.cs:125-136`
  (`Reserved_files_are_recognized_not_concepts`) confirms this at bundle
  root.

### §4 Concept documents

README mapping: `OKF4net.OkfDocument`, `OKF4net.Frontmatter`; §4.2 body
headings → `OkfDocument.Computation()`.

- **S4.1-1** (§4.1, `type` REQUIRED marker) — **Implemented** (Critical).
  `src/OKF4net/OkfDocument.cs:158-166` (`ValidateConformance`): `var hasType
  = value is not null && !value.IsEmptyValue; if (!hasType) throw ...` —
  checks only `type`. `tests/OKF4net.Tests/DocumentTests.cs:91-95`
  (`Conformance_requires_only_type`) confirms a doc with only `type:
  Metric` doesn't throw.
- **S4.1-2** (SHOULD, producers pick descriptive `type` values) — **N/A**.
  Producer-authoring guidance; no code judges `type` value quality.
- **S4.1-3** (MUST, consumers tolerate unknown types) — **Implemented**
  (Major). `src/OKF4net/Validate.cs:14-16`: `"...consumers MUST NOT reject
  a bundle for missing optional fields, unknown types/keys, broken links,
  or missing index.md files."` — no code path checks `Frontmatter.Type`
  against an allowlist; `HasConformantType` (`Validate.cs:368-372`) checks
  only presence/non-emptiness.
- **S4.1-4** (MAY, derive title from filename) — **Implemented** (Minor).
  `src/OKF4net/IndexGenerator.cs:232-233`: `var stem =
  Path.GetFileNameWithoutExtension(child); var title = doc.Frontmatter.Title
  ?? stem;` — falls back to filename stem in the index generator (not on
  `Frontmatter` itself).
- **S4.1-5** (MAY, extension keys) — **Implemented** (Minor).
  `src/OKF4net/Frontmatter.cs:58-59`: `public YamlValue? Get(string key) =>
  _map.Get(key);` — the full parsed map is retained; any extension key
  parses and is retrievable.
- **S4.1-6** (SHOULD, preserve unknown keys on round-trip) —
  **Implemented** (Major). `src/OKF4net/Frontmatter.cs:124-125`
  (`ExtensionKeys`): `_map.Keys.Where(k =>
  !KnownKeys.Contains(k, StringComparer.Ordinal)).ToList()`, and
  `OkfDocument.Serialize()` writes the full map back out. Test:
  `tests/OKF4net.Tests/DocumentTests.cs:109-121`
  (`Unknown_keys_are_preserved_on_roundtrip`).
- **S4.1-7** (MUST NOT, reject unrecognized fields) — **Implemented**
  (Major). Same citation as S4.1-3 — no diagnostic/exception is raised for
  an unrecognized field name anywhere.
- **S4.2-1** (SHOULD, structural markdown over freeform prose) — **N/A**
  (per this skill's own worked example) — pure human-authoring guidance;
  no code judges prose-vs-structure.
- **S4.2-2** (SHOULD, conventional headings `# Schema`/`# Examples`/`#
  Computation` used when applicable) — **Partial** (Major). Only `#
  Computation` is recognized: `src/OKF4net/Internal/ComputationExtractor.cs:12`:
  `private const string Heading = "# Computation";`, used by
  `OkfDocument.Computation()`. A repo-wide search found **no** references
  to `# Schema` or `# Examples` as recognized/extracted headings anywhere
  in `src/` — 1 of 3 conventional headings has code behavior, so this is
  Partial, not Implemented.

### §5 Provenance, trust, and lifecycle

README mapping: `Frontmatter.Sources`/`Generated`/`Verified`/`TrustTier`/
`Status`/`StaleAfter`, `Actor`/`Trust`/`Provenance`/`Lifecycle`.

- **S5.1-1** (§5.1, `sources[].resource` REQUIRED) — **Partial** (Major).
  `src/OKF4net/Provenance.cs:34`: `Resource:
  m.Get("resource")?.AsDisplayString() ?? "",` — a missing `resource`
  silently becomes `""` rather than being rejected.
  `src/OKF4net/Validate.cs:227-230`: `if (src.Resource.Length == 0) {
  diagnostics.Add(new Diagnostic(Severity.Warning, ..., "source entry is
  missing required \`resource\`"));` — detected and named, but only ever
  `Warning`, never `Error`, and `OkfDocument.ValidateConformance()` doesn't
  inspect `sources` at all. Per this skill's `Partial`-vs-`Diverges`
  tie-break rule: this is incomplete enforcement (on a trajectory,
  nothing actively conflicts), not a documented conflict — `Partial`, not
  `Diverges`. No field-specific documented rationale exists for the
  current weighting either way (contrast §13.1 below, which has one).
- **S5.1-2** (§5.1, `id` SHOULD be present when body cites source) —
  **N/A**. Producer-authoring guidance about what to include when writing
  a `sources` entry (parallel to S4.1-2's "producers SHOULD pick
  descriptive `type` values") rather than a checkable consumer-side
  behavior. `src/OKF4net/Provenance.cs:33`: `Id:
  m.Get("id")?.AsDisplayString(),` — parsed as a bare optional field, with
  no code anywhere correlating it against body footnote citations to
  enforce or warn on this specific SHOULD.
- **S5.1-3** (§5.1, per-entry `usage_window` override) — **Missing**
  (Minor). `src/OKF4net/Provenance.cs:7` — the `Source` record has no
  `UsageWindow` field (`Id, Resource, Title, Author, UsageCount,
  LastModified` only); `UsageWindow` is parsed exclusively at the top
  level (`Provenance.cs:45-46`, wired at `Frontmatter.cs:83`) with no
  per-entry override path.
- **S5.1-4** (§5.1, `usage_count` interpreted as liveness/trend not a
  ranking score) — **N/A**. No ranking/scoring code anywhere in `src/`
  reads `UsageCount` at all (parsed and passed through only,
  `Provenance.cs:37`) — the interpretive guidance has no consumer logic to
  apply to.
- **S5.1-5** (§5.1, consumer MAY recurse into a source's own `sources` for
  credibility propagation) — **Missing** (Minor). `Frontmatter.Sources` is
  read only by `OkfDocument.cs` (citation-fallback logic) and
  `Validate.cs:225` (linting) — no code path resolves a `Source.Resource`
  back into another OKF concept and reads *its* `sources`.
- **S5.2-1** (§5.2, `generated.by` REQUIRED) — **Partial** (Major).
  `src/OKF4net/Trust.cs:7`: `public readonly record struct
  Stamp(Actor? By, string? At);` (doc comment: "`By` is null when the
  mapping omitted the (required) `by` key" — parsing tolerates absence).
  `src/OKF4net/Validate.cs:161-163`: `if (g.By is null) {
  diagnostics.Add(new Diagnostic(Severity.Warning, ..., "generated is
  missing required \`by\`"));` — same pattern as S5.1-1: detected and
  named, only `Warning`, no field-specific documented rationale.
  `Frontmatter.cs:23`'s `RequiredKeys` (the stricter producer-side
  `Validate()`) is `["type","title","description"]` only — `generated.by`
  isn't covered there either. Same `Partial`-not-`Diverges` reasoning as
  S5.1-1 applies: incomplete enforcement, not an active conflict.
- **S5.2-2** (§5.2, bare `{by,at}` mapping normalizes to a one-element
  list) — **Implemented** (Major — **also gates §11 conformance**, per
  §11's "MUST treat a bare `verified` mapping as a one-element list
  (§5.2)" bullet, which is a pointer to this statement, not a separate
  §11 row). `src/OKF4net/Trust.cs:30-35`:
  ```csharp
  public static IReadOnlyList<Stamp> ParseVerified(YamlValue? value) => value switch
  {
      YamlMapping m => [StampFrom(m)],
      YamlSequence seq => seq.Items.OfType<YamlMapping>().Select(StampFrom).ToList(),
      _ => [],
  };
  ```
  A bare `YamlMapping` normalizes to a one-element list exactly as
  required. Test: `tests/OKF4net.Tests/TrustTests.cs:35-40`
  (`ParseVerified_bare_mapping_becomes_one_element_list`).
- **S5.3-1** (§5.3, concept with no trust frontmatter still consumable,
  MUST NOT reject) — **Implemented** (Major — **also gates §11
  conformance**, per §11's "MUST NOT reject a concept for missing any
  optional family (§5.3)" bullet, a pointer to this statement, not a
  separate §11 row). `src/OKF4net/Validate.cs:8-20` (class doc):
  "`BundleValidator.Validate` reports only true §11 violations as
  `Severity.Error`; all softer issues are `Severity.Warning` or
  `Severity.Info`." `ValidateConformance()` checks only non-empty `type`;
  `Trust.ParseGenerated`/`ParseVerified` return `null`/`[]` for absent
  data, `Trust.DeriveTier([])` → `TrustTier.Unverified`, and
  `Lifecycle.From(null, null)` defaults to `Stable` — no trust field is
  ever required to load or validate.

### §6 Cross-linking and paths

README mapping: `OKF4net.LinkScanner`, `Bundle.LinksFrom`/`Backlinks`.

- **S6.1-1** (MAY, absolute/relative markdown links) — **Implemented**
  (Minor). `src/OKF4net/Links.cs:12-16` (doc comments on the `Absolute`/
  `Relative` enum members) plus `ConceptLink.Classify` (Links.cs:36-60) and
  `ConceptLink.Resolve` (Links.cs:71-76). Tests:
  `tests/OKF4net.Tests/LinksTests.cs:44-75`.
- **S6.1-2** (MUST, tolerate broken links) — **Implemented** (Major).
  `src/OKF4net/Bundle.cs:246` (doc comment): `"All broken internal links...
  Broken links are permitted by the spec (§6.1) — this is informational."`
  A link whose target isn't in the concept index is added to the outbound
  list with `Exists=false` rather than dropped or erroring. Test:
  `Broken_links_are_detected_but_not_fatal`,
  `tests/OKF4net.Tests/BundleTests.cs:86-99`.

### §7 Actor convention

README mapping: `OKF4net.Actor.Parse`.

- **S7-1** (MUST, `human:<id>` prefix used, and trust tiers key off it) —
  **Implemented** (Major). `src/OKF4net/Actor.cs:31-40` (`Actor.Parse`):
  ```csharp
  if (raw.StartsWith("human:", StringComparison.Ordinal))
  {
      var id = raw["human:".Length..];
      return new Actor(raw, ActorKind.Human, id.Length == 0 ? null : id, null, null, id.Length > 0);
  }
  if (raw.StartsWith("process:", StringComparison.Ordinal))
  ```
  and `src/OKF4net/Trust.cs:45` (`Trust.DeriveTier`): `return
  verified.Any(s => s.By is { IsHuman: true }) ? TrustTier.HumanReviewed :
  TrustTier.MachineConfirmed;` — trust-tier classification keys
  specifically on `Actor.IsHuman`, not "any verifier." Tests:
  `tests/OKF4net.Tests/ActorTests.cs:7-24`,
  `tests/OKF4net.Tests/TrustTests.cs:53-65`.

### §8 Index files

README mapping: `OKF4net.IndexGenerator`.

- **S8-1** (MAY, `index.md` anywhere incl. root) — **Implemented**
  (Minor). `src/OKF4net/IndexGenerator.cs:386-414` (`DirectoriesToIndex`):
  collects all directories with ≥1 `.md` file at any depth, including
  root, uniformly.
- **S8-2** (MAY, root `index.md` carries `okf_version`, the only permitted
  frontmatter) — **Implemented** (Minor). `src/OKF4net/IndexGenerator.cs:288-291`
  (comment): "The bundle-root index.md is the only place frontmatter is
  spec-sanctioned at all (§12...) — every other index.md must have none
  (§8...)". Test: `Regenerate_preserves_root_index_okf_version_marker`,
  `tests/OKF4net.Tests/IndexTests.cs:50-78`; stray frontmatter elsewhere is
  actively stripped (`tests/OKF4net.Tests/IndexTests.cs:131-153`).
- **S8-3** (SHOULD, entries include linked concept's description) —
  **Implemented** (Major). `src/OKF4net/IndexGenerator.cs:234-236`: `var
  description = doc.Frontmatter.Description ?? string.Empty; ...
  entries.Add(...)`, rendered at line 66-67: `var suffix =
  description.Length == 0 ? string.Empty : $" - {description}";`. Test:
  `tests/OKF4net.Tests/IndexTests.cs:39-41`.
- **S8-4** (MAY, producers auto-generate `index.md`) — **Implemented**
  (Minor). `src/OKF4net/IndexGenerator.cs:95-96`: `public static
  IReadOnlyList<string> RegenerateIndexes(string bundleRoot) =>
  RegenerateIndexesWith(bundleRoot, DefaultSynthesize);`, exposed as `okf
  index <bundle>` (`src/OKF4net.Cli/OkfCli.cs:85`).
- **S8-5** (MAY, consumers synthesize `index.md` on the fly when absent) —
  **Missing** (Minor). `src/OKF4net/Bundle.cs:224-225`: `Bundle` only
  records `index.md` files that already exist on disk; no code path in
  `Bundle`, `IndexGenerator`, or the CLI fabricates an in-memory/ephemeral
  index — `RegenerateIndexesWith` always writes to disk.

### §9 Log files

README mapping: `OKF4net.ChangeLog`.

- **S9-1** (MAY, `log.md` anywhere) — **Implemented** (Minor).
  `src/OKF4net/Bundle.cs:434-461` (`CollectMarkdown`) recurses into every
  subdirectory before classifying by filename (`case LogFilename:
  logFiles.Add(path);`).
- **S9-2** (MUST, `YYYY-MM-DD` date headings) — **Partial** (Major).
  `src/OKF4net/Validate.cs:469-477`: `var log =
  ChangeLog.Parse(text); foreach (var bad in log.InvalidDates()) {
  diagnostics.Add(new Diagnostic(Severity.Warning, path, null, $"log date
  heading is not ISO-8601 \`YYYY-MM-DD\`: ..."));` — fully detected and
  named, but only ever `Warning`; `report.IsConformant` stays `true`.
  Test: `Invalid_log_date_heading_is_a_warning`,
  `tests/OKF4net.Tests/ValidateTests.cs:178-189`, which explicitly asserts
  conformance holds despite the violation. No documented rationale exists
  specific to this downgrade, but per this skill's tie-break rule this is
  incomplete enforcement (detected, named, not yet hard-gated) rather than
  an active conflict, so `Partial` rather than `Diverges`.
  (Note: the fenced worked-example headings `## 2026-05-22`/`## 2026-05-15`
  inside §9's own `log.md` sample were correctly excluded from
  extraction — this row is about the general ISO-8601 MUST, not that
  example content.)

### §10 Attested computations concept

README mapping: `Frontmatter.ComputationContract`, `OkfDocument.Computation()`,
`OKF4net.Attestation` (`AttestationOrchestrator`).

- **S10.2-1** (§10.2, `runtime` REQUIRED) — **Partial** (Major).
  `src/OKF4net/AttestedComputation.cs:30-31`: `Runtime` is a nullable
  field, not enforced as required at the model level.
  `src/OKF4net/Validate.cs:286-292`: missing `runtime` is flagged
  (`"attested computation missing required 'runtime'"`) but only as
  `Severity.Warning`. This is a **documented, section-specific** trade-off:
  `src/OKF4net/Validate.cs:282-285` (comment): `"§10 Attested Computation:
  soft guidance only -- §10 is not part of the §11 conformance floor, so a
  malformed Attested Computation concept still stays conformant (Warning,
  never Error)."` — detected and named, deliberately kept non-blocking;
  Partial (not Diverges) per the skill's own tie-break rule, since it's on
  a clear, explained trajectory rather than actively conflicting.
- **S10.3-1** (§10.3, agent supplies parameter values only, MUST NOT
  author/edit the computation) — **Partial** (Major).
  `src/OKF4net.Attestation/AttestationOrchestrator.cs:48-51` (`RunAsync`)
  takes only a concept id (resolving the sanctioned computation from the
  bundle) plus a name→value dictionary — no parameter lets a caller supply
  or edit computation code, so the MAY/MUST NOT half is enforced by
  construction. But the "attester independently re-derives the binding to
  compare against what actually ran" half is only architecturally
  supported (`AttestationContext`, `src/OKF4net.Attestation/Values.cs:37-42`,
  hands the attester contract/computation/bound/values/receipt) — the
  actual re-derivation lives entirely behind the host-plugged
  `IAttester.AttestAsync` and isn't implemented in this repo, so Partial.

### §11 Conformance

README mapping: `OKF4net.BundleValidator`.

- **S11-1** (declarative condition 1: non-reserved `.md` files have
  parseable frontmatter) — **Implemented** (Critical).
  `src/OKF4net/Validate.cs:122-129`: `foreach (var (path, error) in
  bundle.ParseErrors) { diagnostics.Add(new Diagnostic(Severity.Error,
  path, null, $"unparseable concept document: {error}")); }`. Test:
  `Unparseable_frontmatter_is_an_error`,
  `tests/OKF4net.Tests/ValidateTests.cs:14-27`.
- **S11-2** (declarative condition 2: non-empty `type`) — **Implemented**
  (Critical). `src/OKF4net/Validate.cs:136-143`: `if
  (!HasConformantType(concept.Document)) { diagnostics.Add(new
  Diagnostic(Severity.Error, ..., "missing required frontmatter field
  \`type\`")); }`. Tests: `tests/OKF4net.Tests/ValidateTests.cs:29-53`.
- **S11-3** (declarative condition 3: reserved filenames follow §8/§9
  structure) — **Diverges / Undocumented** (Critical). `ValidateReserved`
  (`src/OKF4net/Validate.cs:375-479`) never emits `Severity.Error` for a
  reserved-file structural violation — every finding is `Severity.Warning`,
  e.g. `Validate.cs:419-423`: `diagnostics.Add(new
  Diagnostic(Severity.Warning, path, null, "index.md should not contain
  frontmatter (§8)"));`, confirmed by
  `Nonroot_index_with_frontmatter_is_a_warning`
  (`tests/OKF4net.Tests/ValidateTests.cs:126-139`), which asserts
  `report.IsConformant` stays `true`. Worse, a reserved file that fails to
  *parse at all* is silently skipped with no diagnostic:
  `Validate.cs:404-407`: `catch (DocumentParseException) { continue; }`.
  This **contradicts the class's own docstring** (`Validate.cs:11-13`,
  listing "(3) reserved files follow their structure when present" as a
  conformance condition) and the project's own planning doc,
  `docs/superpowers/plans/2026-07-27-okf-v0.2-core-and-cli.md:18`: "**Error
  severity is reserved for §11 conformance only** (unparseable frontmatter,
  missing/empty `type`, **malformed reserved files**)" — malformed reserved
  files were meant to be `Error`-level; the shipped code treats them as
  `Warning` or silently ignores them. No README/CHANGELOG/design-doc
  citation justifies the downgrade — this is a genuine, undocumented gap
  in one of §11's own three declarative conditions.
- **S11-4** (SHOULD, producers follow §5-§10 when families present) —
  **Implemented** (Major). Throughout `Validate.cs:158-326` the validator
  actively checks every §5-§10 family when present, e.g.
  `Validate.cs:161-163` (quoted under S5.2-1).
- *(Pointer, not a separate row)* — "MUST treat a bare `verified` mapping
  as a one-element list (§5.2)" → see **S5.2-2** above, annotated as also
  gating §11.
- *(Pointer, not a separate row)* — "MUST NOT reject a concept for missing
  any optional family (§5.3)" → see **S5.3-1** above, annotated as also
  gating §11.
- **S11-5** (SHOULD, derive trust tiers/staleness only from specified
  fields) — **Implemented** (Major). `src/OKF4net/Validate.cs:257-270`
  derives staleness solely via `fm.Lifecycle`/`lc.IsStale(today)`, and
  `Lifecycle.From` builds exclusively from `status`/`stale_after`. Test:
  `Stale_concept_warns_using_injected_clock`,
  `tests/OKF4net.Tests/ValidateTests.cs:357-364`.
- **S11-6** (SHOULD, surface not silently drop a failing attestation;
  §10.5 is informative-only, so this is its own §11 statement, not a
  pointer) — **Implemented** (Major), but **not by `BundleValidator`**
  (which does only static structural checks and has no attestation verdict
  to surface or drop) — by `OKF4net.Attestation`'s
  `AttestationOrchestrator.RunAsync`
  (`src/OKF4net.Attestation/AttestationOrchestrator.cs:184`): `return new
  AttestationOutcome(displayable, verdict, receipt, receiptShapeOk, stale,
  reasons, error);` — the outcome, including a failing verdict and its
  reasons, is always returned, never dropped. Worth noting as a nuance:
  the README's §11 row names only `BundleValidator`, but this particular
  §11 statement is actually satisfied by the §10-mapped type instead.
- **S11-7** (SHOULD, treat all other constraints as soft guidance) —
  **Implemented** (Major). This is the organizing principle of the whole
  file: `Validate.cs:22-32` (`Severity` enum) reserves `Error` for the
  three declarative conditions; `Validate.cs:18-20` (class doc):
  "`BundleValidator.Validate` reports only true §11 violations as
  `Severity.Error`; all softer issues are `Severity.Warning` or
  `Severity.Info`."
- **S11-8** (MUST NOT reject a bundle for: missing optional fields,
  unknown `type` values, unknown extra keys, broken cross-links, missing
  `index.md`) — **Implemented** (Critical). Each sub-clause verified: missing
  optional fields → Warning only (`Validate.cs:145-156`,
  `Missing_recommended_fields_are_warnings`,
  `tests/OKF4net.Tests/ValidateTests.cs:55-71`); unknown `type` values →
  `HasConformantType` never checks against a known-type list; unknown extra
  keys → no rejection path, `ExtensionKeys` preserves them instead; broken
  cross-links → `Validate.cs:352-360`, `Severity.Info`, test
  `Broken_link_is_info_not_error_or_warning`
  (`tests/OKF4net.Tests/ValidateTests.cs:203-216`); missing `index.md` →
  `ValidateReserved` only iterates existing `bundle.IndexFiles` — an empty
  collection produces zero diagnostics.

### §12 Versioning

README mapping: `Bundle.OkfVersion`, `OKF4net.OkfSpec.Version`.

- **S12-1** (MAY, declare `okf_version` in root `index.md`) —
  **Implemented** (Minor). `src/OKF4net/Bundle.cs:278`: `public string?
  OkfVersion => _okfVersion;`, and `Bundle.cs:315`: `return
  doc.Frontmatter.Get("okf_version")?.AsDisplayString();`. Test:
  `Okf_version_read_from_root_index`,
  `tests/OKF4net.Tests/BundleTests.cs:138-146`.
- **S12-2** (SHOULD, best-effort consumption of an unrecognized declared
  version rather than refusal) — **Implemented** (Major). No code path in
  `Bundle.cs` compares `OkfVersion` against `OkfSpec.Version` or gates
  `Bundle.Load` — `OkfVersion` is stored purely as an informational
  string. Test loads a bundle declaring `okf_version: "0.1"` (older than
  `OkfSpec.Version = "0.2"`) and asserts `Bundle.Load` succeeds with no
  exception (`tests/OKF4net.Tests/BundleTests.cs:143-145`).

### §13 Changes from v0.1

README mapping: `Frontmatter.LastChangedAt` (falls back to legacy
`timestamp`), `OkfDocument.Sources()` (falls back to legacy `# Citations`).

- **S13.1-1** (`timestamp` superseded by `generated.at`; consumers MAY
  fall back to legacy `timestamp`) — **Diverges / Intentional** (Major).
  `src/OKF4net/Frontmatter.cs:111`: `public string? LastChangedAt =>
  GeneratedAt ?? Timestamp;` implements the fallback (test:
  `LastChangedAt_prefers_generated_at_then_falls_back_to_timestamp`,
  `tests/OKF4net.Tests/FrontmatterTests.cs:108-117`).
  `src/OKF4net/Validate.cs:277-279`: `if (fm.Generated is null &&
  fm.Timestamp is not null) { diagnostics.Add(new
  Diagnostic(Severity.Warning, ..., "\`timestamp\` is a legacy field;
  prefer \`generated.at\`")); }` — flagged as `Warning`, confirmed by
  `Legacy_timestamp_is_a_warning`
  (`tests/OKF4net.Tests/ValidateTests.cs:407-413`, which explicitly asserts
  `Severity.Info` is absent). **Field-specific documented rationale**:
  `docs/superpowers/specs/2026-07-27-okf-v0.2-upgrade-design.md:97-107`
  (item 10) records the severity being **revised from Info to Warning on
  2026-07-29**, explicitly to make the two §13.1 renames "symmetric" after
  reviewing a Google Cloud blog post on the v0.2 trust layer — a real,
  tracked design decision, not an accident.
- **S13.1-2** (body `# Citations` superseded by `sources`; consumers
  SHOULD read `sources`, MAY still parse legacy `# Citations`) —
  **Diverges / Intentional** (Major). `src/OKF4net/OkfDocument.cs:197-214`
  (`Sources()`) prefers `Frontmatter.Sources`, falls back to
  `Citations()`; `UsesLegacyCitations()` (`OkfDocument.cs:224`) flags the
  fallback path. `src/OKF4net/Validate.cs:272-274`: `if
  (concept.Document.UsesLegacyCitations()) { diagnostics.Add(new
  Diagnostic(Severity.Warning, ..., "body \`# Citations\` is legacy; move
  provenance to the \`sources\` frontmatter field")); }` — proven
  end-to-end by the byte-exact golden fixture
  `tests/fixtures/golden/validate-v02.out:3`: `[warning]
  tests/fixtures/okf_v02/metrics/legacy.md: body \`# Citations\` is
  legacy; move provenance to the \`sources\` frontmatter field`. Same
  design-doc citation as S13.1-1 (item 10) documents both renames as
  "deliberately equally weighted" — not a quieter, "simple rename" for
  `timestamp` vs. a heavier one for `# Citations`.

---

## Notes on extraction methodology (for audit)

- Fenced code blocks were excluded from statement extraction, including
  §9's own worked `log.md` example (`## 2026-05-22`/`## 2026-05-15`) — no
  report row above is attributed to that example content.
- §11's pointer-collapsing rule was applied to exactly two bullets ("MUST
  treat a bare `verified` mapping...(§5.2)" and "MUST NOT reject...missing
  any optional family (§5.3)") — both inherited into their pointed-to
  section's row (S5.2-2, S5.3-1) rather than creating duplicate §11 rows.
  §11's citation to §10.5 ("SHOULD surface...a failing attestation
  (§10.5)") was **not** treated as a pointer, per the informative-only
  exception (§10.5 is explicitly labeled "informative, not normative" in
  the spec) — it stayed as its own §11 statement (S11-6).
- §11 yields 8 of its own atomic statements (S11-1..S11-8, i.e. the three
  declarative conditions plus five non-pointer MUST/SHOULD bullets),
  matching the skill's own worked-example count.
